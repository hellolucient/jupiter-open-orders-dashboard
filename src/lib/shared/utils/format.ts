export function formatNumber(value: number | undefined, decimals: number = 0, token?: string): string { if (typeof value === "undefined") return "0"; if (token === "SOL") { return value.toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 }); } if (token === "CHAOS" || token === "LOGOS") { return Math.round(value).toLocaleString("en-US", { minimumFractionDigits: 0, maximumFractionDigits: 0 }); } return value.toLocaleString("en-US", { minimumFractionDigits: decimals, maximumFractionDigits: decimals }); } export function formatPrice(value: number | undefined): string { if (typeof value === "undefined") return "0.00"; if (value > 0 && value < 0.000001) { return value.toLocaleString("en-US", { minimumFractionDigits: 12, maximumFractionDigits: 12 }); } return value.toLocaleString("en-US", { minimumFractionDigits: 6, maximumFractionDigits: 6 }); } export function formatCurrency(value: number | undefined): string { if (typeof value === "undefined") return "0.00"; return value.toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 }); }
